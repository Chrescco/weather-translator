const kelvin = 0;     //assignin a variable to kelvin
const celsius = kelvin - 273;     //assigning celsius the result of 293 - 273 
let fahrenheit = 68;   //assigning a value to fahrenheit Celsius * (9/5) + 32
fahrenheit = Math.floor(fahrenheit);   //rounding of the number to a 'whole' number
console.log(`The temperature is ${fahrenheit} degrees Fahrenheit.`)
